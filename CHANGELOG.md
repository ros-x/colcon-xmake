# Changelog

## 0.1.0

- Initial public scaffolding for xmake-based colcon tasks.
- Added `xmake` and `ros.ament_xmake` task entry points.
- Added timeout and argument validation behavior.
- Added JUnit export for `xmake test`.
- Added unit tests and CI matrix baseline.
